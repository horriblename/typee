;exit=46

(def toStr [t] (case t [
	('one x) (i64ToStr x)
	('two x) (i64ToStr x)
	('three x) (i64ToStr x)
]))

(def adder [t]
	(fn [n] (case t [
		('one x) (+ (+ x 1) n)
		('two x) (+ (+ x 2) n)
		('three x) (+ (+ x 3) n)
	])))

(def main [] (
	let [
		t ('three 42)
		add (adder t)
	]
		(add 1)))
